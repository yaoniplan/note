- #### Write a conditional judgment in Linux Bash
    - `if [[ "$numberOfFiles" -gt 30 ]]; then`
- ***Notes***
    - `if [ "$slug" != "null" ] && [ -n "$slug" ]; then` # Use two separate `[` commands combined with `&&` outside the brackets (POSIX-compliant)
    - `-gt` # Greater than
- ***References***
    - ChatGPT
- ---
